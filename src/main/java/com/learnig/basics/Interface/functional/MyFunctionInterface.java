package com.learnig.basics.Interface.functional;

// annotation enforce the interface to have only one abstract method
@FunctionalInterface
public interface MyFunctionInterface {
    public int add(int a, int b);
}
